# 🛠️ คู่มือระบบ Database Triggers & Functions (RPC)
*เอกสารฉบับนี้จัดทำขึ้นเป็นภาษาไทยเพื่อให้พนักงานและ AI ตัวถัดไปเข้าใจสถาปัตยกรรมฐานข้อมูลและการแก้ไขบั๊กรับลอตสินค้าอัตโนมัติ*

---

## 🚨 บั๊กและ Trigger เจ้าปัญหา: `trigger_sync_stock`

ในการทำงานก่อนหน้านี้ ระบบพบปัญหาว่าเมื่อ **สร้างลอตสินค้าใหม่จากหลังบ้าน (Web Admin)** ยอดการตรวจรับจริง (`received_qty`) ในตาราง `stock_lot_items` จะถูกตั้งค่าให้เท่ากับยอดสั่ง (`expected_qty`) ทันที ทำให้ลอตสินค้าแสดงสถานะเป็น **"ครบแล้ว" (COMPLETED)** โดยที่พนักงานสาขายังไม่ได้ใช้เครื่อง PDA สแกนบาร์โค้ดหรือติกแท็ก RFID จริงๆ

### 🔍 สาเหตุและพฤติกรรมของ Trigger
* **ชื่อ Trigger:** `trigger_sync_stock`
* **ตารางที่ผูกไว้:** `public.stock_lot_items`
* **การทำงานที่เป็นปัญหา:** เมื่อมีแถวข้อมูลใหม่ถูกเพิ่มเข้ามา (INSERT) ตัว Trigger นี้จะทำการเขียนยอด `received_qty = expected_qty` โดยอัตโนมัติ
* **วิธีการแก้ไข:** ต้องทำการยกเลิกและลบ Trigger ตัวนี้ทิ้งเพื่อให้แอปพลิเคชัน PDA เป็นผู้ส่งยอดจริงขึ้นไปเขียนทับแทน ผ่านคำสั่ง SQL ด้านล่างนี้:
  ```sql
  DROP TRIGGER IF EXISTS trigger_sync_stock ON public.stock_lot_items;
  ```

---

## 💾 รายชื่อ Database Functions (RPC) ที่สำคัญในระบบ

ระบบคลังสินค้าและการขายนี้ใช้ Postgres Function (RPC) บน Supabase ในการประมวลผลธุรกรรมที่ต้องการความแม่นยำสูง (Transaction Safety) เพื่อหลีกเลี่ยงการเขียนโค้ดอัปเดตหลายตารางบน Client โดยมีฟังก์ชันหลักดังนี้:

### 1. `public.submit_receive_goods` (ใช้สำหรับการรับสินค้า)
* **พารามิเตอร์:**
  * `p_branch_id` (bigint): รหัสสาขาที่รับของ
  * `p_branch_name` (text): ชื่อสาขา
  * `p_user_id` (uuid): รหัสพนักงาน
  * `p_user_name` (text): ชื่อพนักงาน
  * `p_lot_id` (bigint): รหัสลอต (ถ้ามี)
  * `p_items` (jsonb): รายการสินค้าและแท็ก RFID เช่น `[{"product_id": 29686, "qty": 5, "rfids": ["RFID001", "RFID002"]}]`
* **ขั้นตอนการทำงานภายใน (Workflow):**
  * **[RFID]:** วนลูปบันทึกรหัสแท็ก RFID ลงตาราง `product_rfid_tags` และตั้งค่าสถานะเป็น `IN_STOCK` (ข้ามรหัสที่ซ้ำด้วย `ON CONFLICT DO NOTHING`)
  * **[Lot Sync]:** หากรับแบบมีลอต (`p_lot_id IS NOT NULL`) จะทำการบวกยอดเพิ่มเข้าไปใน `stock_lot_items.received_qty`
  * **[Stock Update]:** บวกจำนวนสต๊อกสินค้าหลักเข้าคลังในตาราง `stock` ของสาขานั้นๆ
  * **[Temp Receiving]:** บันทึกยอดสะสมไว้ในตารางชั่วคราว `stock_receiving`
  * **[History Log]:** บันทึกประวัติการรับเข้าลงตาราง `stock_movements` โดยใช้ประเภท `'IN'` และบันทึกหมายเหตุเป็นชื่อสาขาปลายทาง

### 2. `public.receive_stock_transaction` (สำหรับการรับของจากการโอนย้าย)
* **หน้าที่:** ประมวลผลธุรกรรมเมื่อมีการทำรับสต๊อกที่โอนย้ายระหว่างสาขา (Stock Transfer)
* **การทำงาน:** ปรับปรุงสถานะตารางโอนย้ายสินค้า (`stock_transfers` และ `stock_transfer_items`) พร้อมทั้งตัดสต๊อกจากสาขาต้นทางมาเพิ่มเข้าสาขาปลายทาง และลงบันทึกใน `stock_movements`

### 3. `public.apply_sale_stock` (สำหรับการหักสต๊อกตอนขาย)
* **หน้าที่:** ทำงานอัตโนมัติเมื่อทำรายการสั่งซื้อเสร็จสิ้นผ่านระบบขายหน้าร้าน (POS)
* **การทำงาน:** ค้นหาสินค้าตามรหัสสแกนหรือ RFID แล้วทำการปรับลดจำนวนสต๊อกในคลังของสาขาที่ขายจริงลง พร้อมเปลี่ยนสถานะ RFID แท็กชิ้นนั้นๆ เป็น `'SOLD'` (ขายแล้ว)

### 4. `public.handle_new_user` (ระบบซิงค์ข้อมูลพนักงาน)
* **หน้าที่:** ทำหน้าที่เป็น Trigger จับคู่กับตารางระบบหลักของ Supabase `auth.users`
* **การทำงาน:** เมื่อมีการสมัครสมาชิกหรือสร้างผู้ใช้งานใหม่ในระบบระบบจะสร้างโปรไฟล์ในตาราง `public.profiles` อัตโนมัติพร้อมกำหนด Role พนักงานเริ่มต้นให้ทันที

---

## 📌 ข้อแนะนำสำหรับ AI ตัวถัดไป (Developer Notes)
1. **ห้ามนำ Trigger กลับมาใช้งานโดยไม่จำเป็น:** หากแก้ไขระบบรับของหลังบ้าน ห้ามเขียน Trigger ซ้ำซ้อนที่ไปดึงยอดสั่งซื้อมาตั้งต้นให้พนักงาน เนื่องจากแอป PDA ถูกออกแบบมาเพื่อรับข้อมูลจริงทีละชิ้น
2. **การ Commit ยอดรับ:** ในเครื่อง PDA การบันทึกรับของแบบบาร์โค้ดจะส่งค่าไปยัง `/stock-receiving/commit` (ซึ่งจะเรียกใช้ RPC `submit_receive_goods` โดยส่งค่า `rfids: []` เป็นอาร์เรย์ว่าง)
3. **การล้างยอดชั่วคราว:** เมื่อการรับส่งลอตเสร็จสิ้น สมบูรณ์แล้ว ต้องเรียกฟังก์ชันเคลียร์ตาราง `stock_receiving` ของลอตนั้นๆ ทันทีเพื่อความสะอาดของข้อมูล
